<%- include('./partials/header') %>
<%- include('./partials/navbar') %>

<div class="container mt-4">
    <h1 class="text-center mb-4">Vanlige Spørsmål (FAQ)</h1>
    
    <div class="accordion" id="faqAccordion">
        <% faqs.forEach((faq, index) => { %>
            <div class="card">
                <div class="card-header" id="heading<%= index %>">
                    <h2 class="mb-0">
                        <button class="btn btn-link btn-block text-left" type="button" data-toggle="collapse" data-target="#collapse<%= index %>" aria-expanded="<%= index === 0 ? 'true' : 'false' %>" aria-controls="collapse<%= index %>">
                            <%= faq.spørsmål %>
                        </button>
                    </h2>
                </div>

                <div id="collapse<%= index %>" class="collapse <%= index === 0 ? 'show' : '' %>" aria-labelledby="heading<%= index %>" data-parent="#faqAccordion">
                    <div class="card-body">
                        <%= faq.svar %>
                    </div>
                </div>
            </div>
        <% }); %>
    </div>
    
    <div class="mt-5">
        <h3>Fortsatt spørsmål?</h3>
        <p>Hvis du ikke finner svar på spørsmålet ditt her, vennligst kontakt oss på <a href="mailto:support@it-utdanning.no">support@it-utdanning.no</a>.</p>
    </div>
</div>

<%- include('./partials/footer') %>
