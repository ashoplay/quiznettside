<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
    <div class="container">
        <a class="navbar-brand" href="/">Quiz Nettside</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/">Hjem</a>
                </li>
                <% if (typeof user !== 'undefined') { %>
                <li class="nav-item">
                    <a class="nav-link" href="/quizzes">Quizzer</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/quizzes/create">Lag Quiz</a>
                </li>
                <% if (user.rolle === 'admin') { %>
                <li class="nav-item">
                    <a class="nav-link" href="/admin" style="color: #ffff00;">
                        <i class="fas fa-cogs mr-1"></i> Admin
                    </a>
                </li>
                <% } %>
                <% } %>
                <li class="nav-item">
                    <a class="nav-link" href="/faq">FAQ</a>
                </li>
            </ul>
            <ul class="navbar-nav">
                <% if (typeof user !== 'undefined') { %>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                        aria-haspopup="true" aria-expanded="false">
                        <% if (user.rolle === 'admin') { %>
                            <i class="fas fa-user-shield mr-1"></i>
                        <% } %>
                        <%= user.brukernavn %>
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="/profile">
                            <i class="fas fa-user mr-1"></i> Min Profil
                        </a>
                        <a class="dropdown-item" href="/quizzes/mine/quizzer">
                            <i class="fas fa-list mr-1"></i> Mine Quizzer
                        </a>
                        <% if (user.rolle === 'admin') { %>
                        <a class="dropdown-item" href="/admin">
                            <i class="fas fa-cogs mr-1"></i> Admin Panel
                        </a>
                        <% } %>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="/logout">
                            <i class="fas fa-sign-out-alt mr-1"></i> Logg ut
                        </a>
                    </div>
                </li>
                <% } else { %>
                <li class="nav-item">
                    <a class="nav-link" href="/login">Logg inn</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/register">Registrer deg</a>
                </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
